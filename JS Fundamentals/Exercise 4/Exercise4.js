let a = 1, b = 6;
let result = `${a} + ${b} is ${a + b < 10 ? 'less than 10' : 'greater than or equal to 10'}`;
console.log(result);

// The '+=' operator is used to concatenate the strings
// but with this the string would display / run twice so the above is a simplified version that will still output the same result

